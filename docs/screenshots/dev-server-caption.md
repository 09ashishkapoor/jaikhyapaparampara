**Dev server screenshot** — shows the `npm run serve` command output and the local preview URL. Use this image to confirm the dev server is running and to locate the article preview path.

To generate a PNG from the provided base64 file:

```bash
base64 --decode docs/screenshots/dev-server.png.base64 > docs/screenshots/dev-server.png
```
