**Article preview screenshot** — annotated preview of a sample article showing `transcript-container` and `speaker-block` usage. Use this when reviewing visual formatting of dialogue.

To generate a PNG from the provided base64 file:

```bash
base64 --decode docs/screenshots/article-preview.png.base64 > docs/screenshots/article-preview.png
```
