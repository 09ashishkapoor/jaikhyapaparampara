---
layout: base
title: Sacred Wisdom & Guru Stories | Jai Khyapa Parampara Blog
pagination:
  data: collections.articles
  size: 10
  alias: articles
permalink: "/blog/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber + 1 }}/{% endif %}index.html"
---

<style>
.blog-header{padding:3rem 0 2rem 0;text-align:center;}
.blog-title{font-family:'Philosopher',sans-serif;color:var(--accent-color);font-size:2.8rem;margin-bottom:1rem;}
.blog-description{font-size:1.2rem;color:var(--text-dark);max-width:800px;margin:0 auto;}

/* Tags Styles */
.tags-container {
    max-width: 1000px;
    margin: 1rem auto 2rem;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.8rem;
}
.tag-filter {
    background: rgba(45, 15, 24, 0.4);
    border: 1px solid var(--border-gold);
    color: var(--text-dark);
    padding: 0.4rem 1rem;
    border-radius: 20px;
    font-size: 0.9rem;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
}
.tag-filter:hover, .tag-filter.active {
    background: var(--accent-color);
    color: white;
    border-color: var(--accent-color);
}

/* Search Styles */
.search-container {
    max-width: 600px;
    margin: 2rem auto;
    position: relative;
}
.search-input {
    width: 100%;
    padding: 1rem 1.5rem;
    background: rgba(45, 15, 24, 0.4);
    border: 1px solid var(--border-gold);
    border-radius: 30px;
    color: var(--accent-bright);
    font-size: 1.1rem;
    outline: none;
    transition: all 0.3s ease;
}
.search-input:focus {
    border-color: var(--accent-color);
    box-shadow: 0 0 15px rgba(139, 38, 53, 0.3);
}
.search-input::placeholder {
    color: var(--text-dark);
    opacity: 0.6;
}
.no-results {
    display: none;
    text-align: center;
    padding: 3rem;
    color: var(--text-dark);
    font-size: 1.2rem;
}

.articles-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(350px,1fr));gap:2rem;padding:3rem 0;}
.article-card{background:linear-gradient(135deg,rgba(45,15,24,0.4) 0%,rgba(31,11,17,0.4) 100%);border:1px solid var(--border-gold);border-radius:8px;padding:2rem;transition:all 0.3s ease;}
.article-card:hover{transform:translateY(-5px);border-color:var(--accent-color);box-shadow:0 10px 30px var(--shadow-colored);}
.article-card-category{display:inline-block;background:transparent;border:1px solid var(--accent-color);color:var(--accent-color);padding:0.3rem 1rem;border-radius:15px;font-size:0.85rem;margin-bottom:1rem;}
.article-card-title{font-family:'Philosopher',sans-serif;font-size:1.5rem;color:var(--accent-bright);margin-bottom:0.8rem;line-height:1.3;}
.article-card-title a{color:var(--accent-bright);text-decoration:none;}
.article-card-title a:hover{color:var(--accent-color);}
.article-card-excerpt{color:var(--text-dark);margin-bottom:1rem;line-height:1.6;}
.article-card-meta{color:var(--accent-color);font-size:0.9rem;opacity:0.8;}
.article-card-footer{display:flex;justify-content:space-between;align-items:center;margin-top:1.5rem;padding-top:1rem;border-top:1px solid var(--border-gold);}
.read-more{background:var(--gold-gradient);color:var(--light-bg);padding:0.6rem 1.5rem;border-radius:5px;text-decoration:none;font-weight:700;font-size:0.9rem;transition:all 0.3s ease;}
.read-more:hover{transform:scale(1.05);box-shadow:0 4px 15px rgba(232,196,104,0.4);}

/* Pagination Styles */
.pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    margin: 3rem 0;
}
.pagination-item {
    padding: 0.6rem 1.2rem;
    border: 1px solid var(--border-gold);
    border-radius: 5px;
    color: var(--accent-bright);
    text-decoration: none;
    transition: all 0.3s ease;
}
.pagination-item:hover:not(.disabled) {
    background: var(--accent-color);
    color: white;
    border-color: var(--accent-color);
}
.pagination-item.active {
    background: var(--gold-gradient);
    color: var(--light-bg);
    font-weight: bold;
}
.pagination-item.disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

@media(max-width:768px){
.articles-grid{grid-template-columns:1fr;gap:1.5rem;padding:2rem 0;}
.article-card{padding:1.5rem;}
.blog-title{font-size:2rem;}
}
</style>

<div class="container">
    <div class="blog-header">
        <h1 class="blog-title">Articles & Insights</h1>
        <p class="blog-description">Deep dives into spiritual wisdom, guru stories, and sacred teachings from the Khyapa Parampara tradition.</p>
    </div>

    <div class="tags-container">
        <span style="color: var(--accent-color); font-weight: bold; margin-right: 10px; align-self: center;">Explore Topics:</span>
        <a href="/blog/" class="tag-filter active">All</a>
        <a href="/tags/guru-stories/" class="tag-filter">Guru Stories</a>
        <a href="/tags/sacred-texts/" class="tag-filter">Sacred Texts</a>
        <a href="/tags/philosophy/" class="tag-filter">Philosophy</a>
        <a href="/tags/sadhana/" class="tag-filter">Sadhana</a>
        <a href="/tags/bhairava/" class="tag-filter">Bhairava</a>
        <a href="/tags/kali/" class="tag-filter">Kali</a>
    </div>

    <div class="search-container">
        <input type="text" id="blogSearch" class="search-input" placeholder="Search articles on this page...">
    </div>

    <div id="noResults" class="no-results">
        <p>No articles found matching your search on this page. Try another page or a different term.</p>
    </div>

    <div class="articles-grid" id="articlesGrid">
        {% for article in articles %}
        <div class="article-card" data-title="{{ article.data.title | lower }}" data-description="{{ article.data.description | lower }}" data-tags="{{ article.data.tags | join(' ') | lower }}">
            <span class="article-card-category">{{ article.data.category }}</span>
            <h2 class="article-card-title">
                <a href="{{ article.url }}">{{ article.data.title }}</a>
            </h2>
            <p class="article-card-excerpt">{{ article.data.description }}</p>
            <div class="article-card-meta">
                📅 {{ article.date | dateDisplay }}
                {% if article.data.readingTime %}• ⏱ {{ article.data.readingTime }} min read{% endif %}
            </div>
            <div class="article-card-footer">
                <a href="{{ article.url }}" class="read-more">Read Article →</a>
            </div>
        </div>
        {% endfor %}
    </div>

    <!-- Pagination Controls -->
    {% if pagination.pages.length > 1 %}
    <nav class="pagination">
        {% if pagination.href.previous %}
            <a href="{{ pagination.href.previous }}" class="pagination-item">← Previous</a>
        {% else %}
            <span class="pagination-item disabled">← Previous</span>
        {% endif %}

        {% for pageEntry in pagination.pages %}
            <a href="{{ pagination.hrefs[ loop.index0 ] }}" class="pagination-item {% if page.url == pagination.hrefs[ loop.index0 ] %}active{% endif %}">{{ loop.index }}</a>
        {% endfor %}

        {% if pagination.href.next %}
            <a href="{{ pagination.href.next }}" class="pagination-item">Next →</a>
        {% else %}
            <span class="pagination-item disabled">Next →</span>
        {% endif %}
    </nav>
    {% endif %}
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('blogSearch');
    const articlesGrid = document.getElementById('articlesGrid');
    const articleCards = articlesGrid.getElementsByClassName('article-card');
    const noResults = document.getElementById('noResults');

    searchInput.addEventListener('input', function() {
        const searchTerm = searchInput.value.toLowerCase().trim();
        let visibleCount = 0;

        Array.from(articleCards).forEach(card => {
            const title = card.getAttribute('data-title');
            const description = card.getAttribute('data-description');
            const tags = card.getAttribute('data-tags');

            if (title.includes(searchTerm) || description.includes(searchTerm) || tags.includes(searchTerm)) {
                card.style.display = 'block';
                visibleCount++;
            } else {
                card.style.display = 'none';
            }
        });

        if (visibleCount === 0) {
            noResults.style.display = 'block';
            articlesGrid.style.display = 'none';
        } else {
            noResults.style.display = 'none';
            articlesGrid.style.display = 'grid';
        }
    });
});
</script>
