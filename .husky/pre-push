#!/usr/bin/env sh

echo "Running Playwright validation before push..."
# Exclude performance tests — LCP timing is machine-dependent and runs reliably on CI only
npx playwright test tests/smoke tests/visual tests/accessibility

if [ $? -ne 0 ]; then
  echo "Playwright validation failed. Push blocked."
  echo "Fix the failing tests before pushing."
  exit 1
fi
